Build the installable zip with:

    cp ../../index.html heartland-vial-labels/labels.html
    zip -r heartland-vial-labels.zip heartland-vial-labels

Upload in WP Admin -> Plugins -> Add New -> Upload Plugin, activate,
and the label printer serves at /labels/ on the site.
